#include <stdio.h>
int main(){
  int instances, i;
  double numbers[100];
  printf("Input the number of numbers you want to enter to find the greatest: ");
  scanf("%d", &instances);
  for(i=0,i<=instances,++i){
    numbers[i];
  }
  
  return 0;
}
